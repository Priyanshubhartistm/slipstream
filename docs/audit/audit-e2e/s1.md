# S1 — Program authorization and the L1 money path

**Run:** `audit-e2e` · **Slice:** S1 · **Issue:** #3 · **Freeze SHA:** `b98f0508b01af619f84a6cbb82b6326ebbcd7f9f`

Audited at the freeze SHA. Read scope: whole repository. Filing scope: the 22 files
listed in `docs/runs/audit-e2e/issues/01-s1.md`. Live devnet reads in this document are
read-only `getAccountInfo` / `getProgramAccounts` / `getSignaturesForAddress` calls
against `https://api.devnet.solana.com`, taken at `2026-08-21T19:17Z`. No signer was
held and no transaction was submitted.

## Calibration

> Flag only gaps that affect correctness, the stated requirements, or documented project invariants -- cite file:line evidence for every finding. Do not report stylistic preferences.

## Producer contract — S1-C5, the owner-vs-session-key boundary

Published for S2 (`S2-C10`) and S9 (`S9-C1`). This is the definition S1 owns; S2 checks
that `place_order` / `cancel_order` honour it.

**The boundary, as the program actually enforces it.**

1. `TradingCredit.session_authority` / `.session_expiry`
   (`programs/slipstream/src/state/trading_credit.rs:42-45`) are the *only* two fields
   in the whole program that name a non-owner signer. No other state type has a
   delegate, operator, or session field.
2. `authorize_session` (`programs/slipstream/src/instructions/authorize_session.rs:56-61`)
   is the **only** writer of those two fields outside `initialize_trading_credit`
   (which zeroes them, `initialize_trading_credit.rs:73-74`) and
   `delegate_trading_credit` (which sets them in the same transaction as delegation,
   `delegate_trading_credit.rs:181`). Every one of those writers requires
   `owner.is_signer()` **and** `credit.owner == *owner.key()`.
3. `TradingCredit::is_authorized_signer` (`trading_credit.rs:110-117`) is the sole
   consumer. Grep of the whole program: it is called from exactly two sites,
   `place_order.rs:151` and `cancel_order.rs:56`. **No instruction in S1's owned set
   calls it, and no instruction in S1's owned set has any code path that accepts a
   signer other than the account's recorded `owner`.**
4. Every one of the fifteen owned instructions gates on exactly one `is_signer()` check
   and, where program state carries an `owner`/`authority` field, an equality check
   against it. The table is in `## Cleared` under `S1-C1`.

**The contract S2 and S9 may rely on.**

- A session key can never move collateral: no path in `deposit_collateral`,
  `withdraw_collateral`, `fund_trading_credit`, `withdraw_trading_credit`,
  `close_trading_credit`, `close_user_account`, `propose_authority`,
  `accept_authority`, or `set_market_oracle` consults `session_authority`.
- A session key can never rotate itself: `authorize_session.rs:57` compares against
  `credit.owner`, not against `is_authorized_signer`.
- Attribution is never the signer. Nothing in the owned set writes a signer's key into
  an `owner` field; every `owner` field is written once, at init, from the account's
  PDA-deriving signer (`initialize_user.rs:61`, `initialize_position.rs:85`,
  `initialize_trading_credit.rs:70`).

**Two caveats S2/S9 must not assume away.**

- **The expiry ceiling is not enforced on-chain.** `authorize_session` accepts any
  `i64` (see `S1-07`). "Expiring" is a client-chosen property, not a program invariant.
- **The boundary is defined per `TradingCredit`, and `TradingCredit` is the delegated
  account.** Everything the ER can reach is bounded by `credit.credit`, which only
  owner-signed L1 instructions raise (see `## Cleared`, `S1-C8`). S1 confirms the
  ceiling exists; whether the ER honours it is `S2-C11`'s question, not S1's.

## Findings

### S1-01 · Legacy 56-byte TradingCredit accounts are unreachable by every instruction; funded credit and rent are permanently stranded

- **Status:** CONFIRMED
- **Severity:** P0
- **Tag:** [reachable-now]
- **Class:** S1-C7
- **Evidence:** `programs/slipstream/src/state/trading_credit.rs:51-59`, `programs/slipstream/src/instructions/close_trading_credit.rs:54`, `programs/slipstream/src/instructions/withdraw_trading_credit.rs:46`, `programs/slipstream/src/instructions/fund_trading_credit.rs:57`, `programs/slipstream/src/instructions/delegate_trading_credit.rs:118`, `programs/slipstream/src/instructions/close_trading_credit.rs:13-16`

**What is wrong.** `TradingCredit::from_account_info` and its `_mut` sibling reject any
account whose data is shorter than `TradingCredit::LEN` (96 bytes) with
`ProgramError::InvalidAccountData` (`trading_credit.rs:53-55`, `:68-70`). Every single
instruction that touches a `TradingCredit` loads it through one of those two functions —
all 18 load sites, with no exception and no shorter-layout fallback. Accounts created
under the pre-session-keys 56-byte layout are therefore refused by *every* instruction
that could drain, close, delegate, or repair them: `withdraw_trading_credit`,
`close_trading_credit`, `fund_trading_credit`, `delegate_trading_credit`,
`undelegate_trading_credit`, `authorize_session`, `place_order`, `cancel_order`. The
program has no realloc path for a `TradingCredit` (the only `resize` call anywhere is
`propose_authority.rs:92`, on `GlobalState`). The written invariant this violates is
`close_trading_credit.rs:13-16`, which states the instruction exists precisely to
"migrate a legacy (e.g. 56-byte) credit to the new layout: close the old account" — the
one case it is written for is the one case it structurally cannot serve. Nothing
surfaced it because `close_trading_credit` has **zero** test coverage (unit or
integration), and the one verification script that exercises it,
`keepers/src/verify-close.ts:56-70`, creates a *fresh* 96-byte credit first, so it can
never hit the legacy path it claims to prove.

**Proof.** Exhaustive grep of every `TradingCredit` load site — all route through the
96-byte guard:

```
$ grep -rn "TradingCredit::from_account_info" programs/slipstream/src/
# (paths shortened to basenames, runs of hits on one file collapsed onto one line)
close_trading_credit.rs:54:    let credit = TradingCredit::from_account_info(trading_credit_acc)?;
place_order.rs:149 / :197 / :207 / :331 / :337
cancel_order.rs:54 / :75 / :142
fund_trading_credit.rs:57 / :70
withdraw_trading_credit.rs:46 / :57
authorize_session.rs:56
undelegate_trading_credit.rs:96
initialize_trading_credit.rs:64        <- _mut_or_init, also `data.len() < Self::LEN` at :86-88
delegate_trading_credit.rs:118 / :181
(18 sites; every one is from_account_info / _mut / _mut_or_init)

$ sed -n '51,56p' programs/slipstream/src/state/trading_credit.rs
    pub fn from_account_info(account: &AccountInfo) -> Result<&Self, ProgramError> {
        let data = unsafe { account.borrow_data_unchecked() };
        if data.len() < Self::LEN {
            return Err(ProgramError::InvalidAccountData);
        }
```

Live read-only devnet read, `2026-08-21T19:17:41Z`, program
`7qujfsb4ZPbQHYVZdqiXq1r8tVAMyyukX94obPqXbVwz`,
`getProgramAccounts` filtered `dataSize: 56`, decoded at the legacy layout
(disc 1 / bump 1 / market_index 2 / active_orders 2 / pad 2 / owner 32 / credit 8 /
committed 8). All four are owned by the *program*, so they are NOT delegated — the
delegation-ownership mechanism of K5 does not explain them:

```
== 56-byte DISC_TRADING_CREDIT (legacy layout; TradingCredit::LEN is 96) : 4
  5NiRhAQkbs1VrAJWH32hS4Qd21X3CV4nGboNcKNtijh5 owner=68HCjPJHwgFSsLwC4qe4Rt8UmPSgv9eLY24wDKKYt4GE mkt=0 active_orders=0 credit=50000000 committed=0 lamports=1280640
  95g97C8uGPhD7Y5FZEC1Wqz6T9iv4pbJKFqDkd68v5B8 owner=BxgUCz4kD9CUnpM648yAHNkNDXBxXxEsJVM5Cct66SKb mkt=0 active_orders=0 credit=50000000 committed=0 lamports=1280640
  Dojn38Q5uo51poBLCerpDNHdisH1Et4ExMApYSU22kT8 owner=BXDFvyrbrp9VqLyttWFKSfANC8SELyzsTRZfK1tMDtBm mkt=0 active_orders=0 credit=13163240000 committed=0 lamports=1280640
  EJ9QXMgpTax5URumvuG8JetfMTTX6PSy5N8wMzTDcveo owner=EfLMGSCWTzwnWUUc8MtMyxCGRVnqQzmsaSuEUGvCJPAo mkt=0 active_orders=0 credit=50000000 committed=0 lamports=1280640

$ getSignaturesForAddress (last activity per account)
  5NiRhAQk... 2026-05-30T03:44:51Z   95g97C8u... 2026-05-30T08:02:43Z
  Dojn38Q5... 2026-05-30T08:04:10Z   EJ9QXMgp... 2026-05-30T07:49:24Z
```

USDC is 6-decimal, so the frozen balances are 50.00 + 50.00 + 13,163.24 + 50.00 =
**13,313.24 USDC**, plus 4 × 1,280,640 lamports (≈0.00512 SOL) of rent. Untouched for
84 days. For contrast, the same query at `dataSize: 96` returns exactly one modern
credit, so this is not a decoding artefact: the program is live with both layouts
present and only one of them reachable.

**Blast radius.** Four distinct owner wallets (none of them the operator key
`A5sV4Pkk…`) have lost 13,313.24 USDC of funded trading credit and their rent, with no
on-chain recovery path of any kind: the balance cannot be drained back to
`free_collateral` (`withdraw_trading_credit.rs:46` refuses), the account cannot be
closed for its rent (`close_trading_credit.rs:54` refuses), and it cannot be re-funded
or re-used (`fund_trading_credit.rs:57`, `delegate_trading_credit.rs:118` refuse).
Recovery requires a program upgrade, i.e. it depends on the single concentrated operator
key of K2. On devnet the tokens are worthless; on mainnet this is a permanent,
already-realised loss of user principal, and it recurs on *every* future change to
`TradingCredit::LEN` because the guard is a bare `<` on a `size_of` constant with no
version byte to dispatch on.

**Suggested remediation.** Add a length-tolerant close path: in
`close_trading_credit`, before the typed load, accept `data.len() >= LEGACY_LEN` and
read `owner`/`credit`/`committed`/`active_orders` by fixed offset (they are identical in
both layouts), credit `credit` back to the caller's `UserAccount` and then close. Longer
term, put a layout-version byte in the reserved padding at `trading_credit.rs:34` and
dispatch on it, so a future `LEN` change cannot strand accounts again.

### S1-02 · close_trading_credit destroys a non-zero credit balance, and its own comment claims a recovery path that no code provides

- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** S1-C7
- **Evidence:** `programs/slipstream/src/instructions/close_trading_credit.rs:27-30`, `programs/slipstream/src/instructions/close_trading_credit.rs:58-72`, `programs/slipstream/src/instructions/fund_trading_credit.rs:61-74`

**What is wrong.** `close_trading_credit` gates on `committed == 0` and
`active_orders == 0` (`:59-61`) but deliberately does **not** gate on `credit == 0`. Its
doc comment (`:27-30`) justifies this: *"this does NOT validate against `credit > 0`
because the credit balance … is independently recoverable via the UserAccount
accounting."* That statement is false. `fund_trading_credit` performs a one-way move —
`user.free_collateral -= amount` and `credit.credit += amount` (`fund_trading_credit.rs:68-74`)
— and the **only** instruction that reverses it is `withdraw_trading_credit`, which reads
the balance out of the credit account itself. Zeroing that account (`:64-67`) destroys
the sole record of the claim while the matching USDC stays in the vault. No path
restores `free_collateral` from a closed credit.

**Proof.** Exhaustive grep of every writer of `free_collateral` in the program — none of
them is a "recover a closed credit" path:

```
$ grep -rn "free_collateral" programs/slipstream/src/ | grep -v '//'
initialize_user.rs:62            = 0                      (creation)
deposit_collateral.rs:84-85      += transferred amount
withdraw_collateral.rs:146-147   -= withdrawn amount
fund_trading_credit.rs:68        -= amount                (the debit in question)
withdraw_trading_credit.rs:61-62 += amount   <- reads credit.credit; needs a LIVE credit account
claim_funding.rs:90-101, close_position.rs:228, liquidate_position.rs:223,
settle_trades.rs:175/195/299/307/452, settle_from_log.rs:197/217   (settlement/funding only)
close_user_account.rs:76         != 0 guard (read only)
```

`close_trading_credit` appears nowhere in that list, and no instruction takes both a
closed-credit witness and a `UserAccount`. Against the live deployment there is one
96-byte credit that satisfies every gate this instruction checks and would be silently
emptied by it:

```
MODERN credit Bkcwr5R4jT2ovN7SyGzFnPQo2bTj7MLR3Ji7x9FsyUiY
  owner=B8QxhmyEdTvXFfmuBTcgBFRMkvb56Y3LioWSz9jyDzYU mkt=0
  credit=500000000 (500.00 USDC)  committed=0  active_orders=0
  -> close_trading_credit gates: program-owned OK, committed==0 OK, active_orders==0 OK
```

**Blast radius.** Anyone holding a funded, idle credit who calls `0x1C` loses that
balance permanently; the USDC stays in the market vault as an unclaimable surplus. Rated
P2 rather than P1 because no third party can trigger it and the only shipped UI caller
gates on `state.legacyCredit` (`frontend/src/hooks/use-session.ts:121-123`), so today it
is only reachable through the SDK (`client/src/instructions.ts:609`) or a hand-built
transaction. The gap is a missing guard whose absence is justified by a false claim, not
a live exploit.

**Suggested remediation.** Either add `if credit.credit != 0 { return Err(CreditStillActive) }`
next to the existing idle gates at `close_trading_credit.rs:59`, or take the
`UserAccount` as a fourth account and fold the remaining balance back into
`free_collateral` before zeroing. Delete the recoverability claim at `:27-30` either way.

### S1-03 · deposit_collateral and withdraw_collateral read a Market without pinning its canonical PDA, contradicting the invariant asserted in place_order

- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** S1-C4
- **Evidence:** `programs/slipstream/src/instructions/withdraw_collateral.rs:51-59`, `programs/slipstream/src/instructions/deposit_collateral.rs:65-73`, `programs/slipstream/src/instructions/place_order.rs:104-120`, `programs/slipstream/src/instructions/withdraw_trading_credit.rs:38-49`, `programs/slipstream/src/instructions/authorize_session.rs:48-57`

**What is wrong.** `place_order.rs:104-108` records the invariant explicitly: *"Every
other instruction that reads a Market pins it; this one didn't"* — followed by the fix,
a `find_program_address(&[SEED_MARKET, market_index])` equality check. The claim is
false. `deposit_collateral` and `withdraw_collateral` both check only
`market_acc.owner() == program_id` plus the `DISC_MARKET` byte and then read
`market.quote_vault` out of it (`withdraw_collateral.rs:51-58`,
`deposit_collateral.rs:65-72`); neither derives `SEED_MARKET`. `withdraw_collateral` is
the *only* instruction in the program that moves tokens out of the vault, and the
account it trusts to name the source vault is the unpinned one. Separately, five owned
instructions mutate a `UserAccount` or `TradingCredit` without ever deriving its PDA,
relying entirely on the recorded `owner` field:
`fund_trading_credit.rs:49-60`, `withdraw_trading_credit.rs:38-49`,
`authorize_session.rs:48-57`, `close_trading_credit.rs:50-57`,
`close_user_account.rs:60-75`.

**Proof.** Mechanical check of which `Market` readers derive `SEED_MARKET`:

```
$ for f in $(grep -ln "Market::from_account_info" programs/slipstream/src/instructions/*.rs); do
    echo "$(basename $f)  SEED_MARKET=$(grep -c SEED_MARKET $f)"; done
# (13 output lines, reflowed into two columns and annotated)
initialize_market.rs   SEED_MARKET=3      claim_funding.rs      SEED_MARKET=2
set_market_oracle.rs   SEED_MARKET=2      close_position.rs     SEED_MARKET=2
settle_trades.rs       SEED_MARKET=2      liquidate_position.rs SEED_MARKET=2
place_order.rs         SEED_MARKET=2      settle_from_log.rs    SEED_MARKET=2
crank_twap.rs          SEED_MARKET=0   <- NOT pinned (routed, S1-X01)
compute_funding.rs     SEED_MARKET=0   <- NOT pinned (routed, S1-X02)
execute_trigger.rs     SEED_MARKET=0   <- NOT pinned (routed, S1-X02)
deposit_collateral.rs  SEED_MARKET=0   <- NOT pinned (owned, this finding)
withdraw_collateral.rs SEED_MARKET=0   <- NOT pinned (owned, this finding)
```

Why it is P2 and not P0 today: a forged `Market` is not constructible. `initialize_market`
is the sole creator, is `GlobalState.authority`-gated (`initialize_market.rs:42-65`) and
pins the market PDA (`:96-102`), so the set of `DISC_MARKET` accounts equals the set of
canonical market PDAs. The live deployment has exactly one:

```
GlobalState hyPv4sWbkHPyrVa5zFHkiuqBLCmZdAiZeupieXrEEcL  market_count=1  paused=0
getProgramAccounts dataSize=2064 disc=2 -> 1 account (ECUp8pXz…, market_index=0)
```

The bound is therefore "there is currently only one market," which is exactly the
environment change the P2 bar names.

**Blast radius.** With a second market live, a user could withdraw against market *B*'s
`quote_vault` using `free_collateral` they deposited into market *A*'s vault — the
`UserAccount.free_collateral` pot is global (`user_account.rs:14`) while `quote_vault`
is per-market (`market.rs` `quote_vault`), and nothing in `withdraw_collateral` ties the
market they pass to the market they deposited through. Vault segregation between markets
is not enforced anywhere. The `UserAccount`/`TradingCredit` half is defence-in-depth only:
those `owner` fields are written once from a PDA-deriving signer, so substitution buys an
attacker nothing today.

**Suggested remediation.** Add the same three lines `place_order.rs:114-120` already has
to `deposit_collateral` and `withdraw_collateral`. If cross-market vault segregation is
intended, `withdraw_collateral` additionally needs per-market collateral accounting, not
just a PDA pin — that is a design decision, not a patch. Correct or delete the false
claim at `place_order.rs:107`.

### S1-04 · The global pause is a one-way switch with no clearing instruction, and mod.rs documents a coverage map the program does not have

- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** S1-C10
- **Evidence:** `programs/slipstream/src/instructions/mod.rs:169-176`, `programs/slipstream/src/instructions/mod.rs:103-166`, `programs/slipstream/src/instructions/initialize_global.rs:71`, `programs/slipstream/src/state/global_state.rs:12`, `programs/slipstream/src/instructions/withdraw_collateral.rs:77-80`

**What is wrong.** Two defects in one mechanism.
*(a) No unpause.* `GlobalState.paused` (`global_state.rs:12`) has exactly two writers in
the entire program: `initialize_global.rs:71` sets it to `0` at creation, and
`emergency_undelegate.rs:141` sets it to `1`. The dispatcher at `mod.rs:103-166` lists
all 41 instructions; none of them clears the flag. Once armed, the protocol is halted
permanently with no on-chain remedy short of a program upgrade. Because
`settle_trades.rs:81` and `settle_from_log.rs:60` are both gated on the flag, a pause
also freezes settlement, so `UserAccount.pending_fills` can never drain — and
`withdraw_collateral.rs:78-80` refuses any withdrawal while `pending_fills > 0`. A pause
therefore does not merely stop trading; it makes every user's collateral unwithdrawable
for as long as it lasts, which is forever.
*(b) The documented coverage map is wrong.* `mod.rs:169-170` states *"Non-trivial
trading instructions (place/cancel/settle/liquidate/funding) call this before mutating
state."* Three of the five named categories do not.

**Proof.** Exhaustive grep of call sites and of every write to `paused`:

```
$ grep -rn "ensure_not_globally_paused" programs/slipstream/src/instructions/*.rs
# (paths shortened; the `use` lines are omitted, the 7 call sites are all of them)
delegate_trading_credit.rs:107   deposit_collateral.rs:41   fund_trading_credit.rs:38
place_order.rs:79                place_trigger.rs:57        settle_from_log.rs:60
settle_trades.rs:81
-> 7 call sites. NOT called by: cancel_order, liquidate_position, compute_funding,
   claim_funding, close_position, execute_trigger, withdraw_collateral,
   withdraw_trading_credit, mirror_fills, record_pending_fill, crank_twap.
   The comment names place/cancel/settle/liquidate/funding; only place and settle match.

$ grep -rn "\.paused" programs/slipstream/src/
instructions/mod.rs:172              if global.paused != 0        (read)
instructions/initialize_global.rs:71 state.paused = 0             (write, creation)
instructions/emergency_undelegate.rs:141 global_mut.paused = 1    (write, the only 1)
-> no instruction anywhere writes 0 after creation.
```

Live devnet confirms the flag is currently clear, so nothing is halted today:

```
GlobalState hyPv4sWbkHPyrVa5zFHkiuqBLCmZdAiZeupieXrEEcL
  dataLen=104 disc=1 market_count=1 paused=0 authority=A5sV4PkkVM4gm3rejACvKFgxEMmj8ouGsffSKT5qYVc8
```

**Blast radius.** P2 rather than P1 because the single writer of `paused = 1` is itself
currently unreachable — `emergency_undelegate.rs:25-36` documents that its magic-program
CPI and its `GlobalState` write require mutually exclusive layers, so the instruction
cannot complete on either. That is a documented limitation and is not re-reported here;
what is *not* documented is that if it ever were fixed as written, the first successful
call would strand every user's collateral permanently. The coverage-map half is a
correctness gap in a safety control's own documentation: an operator reading
`mod.rs:169-170` would believe a pause stops liquidations and funding accrual, and it
does not.

**Suggested remediation.** Add an authority-gated `set_pause(bool)` instruction to the
dispatcher and have `emergency_undelegate` use it, so pause is reversible before it is
ever reachable. Then either extend `ensure_not_globally_paused` to the instructions the
comment names, or rewrite the comment at `mod.rs:169-170` to state the real, deliberate
map (deposits and new orders pause; exits, cancels, liquidations and settlement do not).

### S1-05 · initialize_market records quote_vault with no validation, and Market.quote_mint is written all-zero and never read

- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** S1-C12
- **Evidence:** `programs/slipstream/src/instructions/initialize_market.rs:167-170`, `programs/slipstream/src/instructions/deposit_collateral.rs:59-73`, `programs/slipstream/src/instructions/withdraw_collateral.rs:127-143`, `programs/slipstream/src/state/market.rs:1-30`

**What is wrong.** `initialize_market` copies `*quote_vault_acc.key()` into
`market.quote_vault` (`:170`) after performing **no** checks on that account at all: not
that it is owned by the SPL Token program, not that it is an initialized token account,
not what its mint is, and — the load-bearing one — not that its SPL authority is the
`SEED_VAULT_AUTHORITY` PDA that `withdraw_collateral.rs:127-143` will later sign with.
In the same block, `market.base_mint` and `market.quote_mint` are written as `[0u8; 32]`
(`:167-168`) and no instruction in the program ever reads either field. So the "market's
collateral mint" that class S1-C12 asks about does not exist as data; the mint is pinned
only transitively, by SPL Token's own source-mint == destination-mint rule inside the
`Transfer` CPI, exactly as `deposit_collateral.rs:63-64` reasons. That reasoning is
correct for the mint and silent about the authority.

**Proof.** The live market carries zeroed mint fields, and its recorded vault is a
correctly-configured Token account only by construction, not by any on-chain check:

```
== Market ECUp8pXzVLzxjVs8mtKBJma3mdcHf8zSC4cqPeBy8MPy   (read-only, 2026-08-21T19:17Z)
   owner=7qujfsb4ZPbQHYVZdqiXq1r8tVAMyyukX94obPqXbVwz dataLen=2064 disc=2 market_index=0
   base_mint  = 11111111111111111111111111111111   <- all zero
   quote_mint = 11111111111111111111111111111111   <- all zero
   quote_vault= BTWVG5oDmomaMkpdX4xYgfjr1gpEGodW5oPe3k5P71qr

== quote_vault BTWVG5oDmomaMkpdX4xYgfjr1gpEGodW5oPe3k5P71qr  (jsonParsed)
   token program owner = TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA
   {"mint":"Fakb9gPACMBbfQgepdAEmPCYmNU4iKAqQhFKfrDU6gDr",
    "owner":"Hqnwju5Qax1iYqth2i9mgaArweLiG8Tayek3GHzaMMqH",
    "state":"initialized","tokenAmount":{"amount":"655915845954","decimals":6}}
```

The vault's SPL authority is `Hqnwju5Qax…`, which must equal the program's
`vault_authority` PDA for `withdraw_collateral.rs:137-143` to succeed — but that equality
is established nowhere in the program, only by whatever the deploy script happened to
pass at `initialize_market`. Grep confirms no read of the mint fields:

```
$ grep -rn "quote_mint\|base_mint" programs/slipstream/src/
state/market.rs   (struct field declarations)
instructions/initialize_market.rs:167   market.base_mint  = [0u8; 32];
instructions/initialize_market.rs:168   market.quote_mint = [0u8; 32];
-> declared, zeroed once, never read.
```

**Blast radius.** An authority that passes a token account whose SPL authority is not the
`vault_authority` PDA creates a market where every `deposit_collateral` succeeds and
every `withdraw_collateral` fails: user funds enter a vault the program cannot sign for,
and every depositor's collateral is unrecoverable. There is no post-hoc repair —
`set_market_oracle` rotates the feeds but nothing rotates `quote_vault`. The 655,915.85
USDC currently in the live vault is fine; the defence against ever getting this wrong is
absent, and the failure is silent until the first withdrawal.

**Suggested remediation.** In `initialize_market`, before recording the key: assert
`quote_vault_acc.owner() == &pinocchio_token::ID`, unpack it, assert its authority equals
`find_program_address(&[SEED_VAULT_AUTHORITY], program_id).0`, and write its real mint
into `market.quote_mint` instead of zeros. Then have `deposit_collateral` /
`withdraw_collateral` assert `user_token_acc`'s mint equals `market.quote_mint`, which
also closes the aliasing gap in `S1-06`.

### S1-06 · withdraw_collateral does not require quote_vault and user_token_acc to be distinct; a self-transfer debits free_collateral while moving no tokens

- **Status:** SUSPECTED
- **Severity:** P2
- **Class:** S1-C12
- **Evidence:** `programs/slipstream/src/instructions/withdraw_collateral.rs:28-40`, `programs/slipstream/src/instructions/withdraw_collateral.rs:137-149`, `programs/slipstream/src/instructions/deposit_collateral.rs:75-87`

**What is wrong.** `withdraw_collateral` pins `quote_vault_acc` to `market.quote_vault`
(`:56-58`) but places no constraint at all on `user_token_acc` — not an owner check, not
a mint check, and not a distinctness check against `quote_vault_acc`. Pinocchio does no
account-distinctness validation. If the caller passes the vault as *both* the transfer
source and destination, the SPL Token program's `Transfer` handler takes its
`self_transfer` early-return path: it validates the authority (the `vault_authority` PDA,
which the program does sign for at `:136-143`) and then returns `Ok(())` without moving
any tokens. Control returns here and `free_collateral` is debited unconditionally at
`:146-149`. That breaks the class-6 invariant that the token movement and the
`free_collateral` delta always agree.

**Proof.** The two accounts are destructured with no comparison between them and the
debit is unconditional on the CPI having moved anything:

```
$ sed -n '28,40p;136,149p' programs/slipstream/src/instructions/withdraw_collateral.rs
    let [ user_account_acc, owner, quote_vault_acc, user_token_acc,
          vault_authority_acc, _token_program, market_acc, global_state_acc,
          positions @ .. ] = accounts else { ... };
    ...
    let signer_seeds = seeds![SEED_VAULT_AUTHORITY, &vault_auth_bump_bytes];
    Transfer { from: quote_vault_acc, to: user_token_acc,
               authority: vault_authority_acc, amount }
        .invoke_signed(&[Signer::from(&signer_seeds)])?;
    let user_mut = UserAccount::from_account_info_mut(user_account_acc)?;
    user_mut.free_collateral = user_mut.free_collateral.checked_sub(amount)...
```

Not CONFIRMED: the claim depends on SPL Token's `self_transfer` early return, which I
read from the upstream handler's documented behaviour but did not execute here. **What
would settle it:** a Mollusk case in the existing `tests/unit/src/` harness that runs
`withdraw_collateral` with `user_token_acc == quote_vault_acc` and asserts either
(a) the instruction fails, or (b) it succeeds with the vault balance unchanged and
`free_collateral` reduced. That needs an added test file, which this audit-only run must
not create. A devnet `simulateTransaction` would also settle it but needs a funded
`UserAccount` whose owner key this run does not hold.

**Blast radius.** Self-inflicted only: the caller donates their own balance to the vault
as an unclaimable surplus. No third party can trigger it, which is why it is P2 and not
the P0 the class text reserves for aliasing that double-counts a balance or misdirects a
rent refund. The real cost is that the vault's token balance and the sum of all
`free_collateral` silently stop agreeing, so any future solvency check built on that
identity would be wrong.

**Suggested remediation.** One line before the CPI:
`if user_token_acc.key() == quote_vault_acc.key() { return Err(SlipstreamError::InvalidVault.into()) }`.
The mint assertion suggested in `S1-05` is the stronger version and subsumes it.

### S1-07 · authorize_session accepts any session_expiry with no upper bound, so the "expiring" bound the docs rely on is client-chosen

- **Status:** CONFIRMED
- **Severity:** P3
- **Class:** S1-C5
- **Evidence:** `programs/slipstream/src/instructions/authorize_session.rs:52-61`, `programs/slipstream/src/state/trading_credit.rs:110-117`, `docs/06-session-keys.md:62-76`, `frontend/src/hooks/use-session.ts:48`

**What is wrong.** `docs/06-session-keys.md:62-76` presents expiry as one of "three
independent limits" that contain a session key: *"`session_expiry` is a hard wall. After
it passes the key is dead."* The program enforces no wall. `authorize_session` reads the
caller's 8 bytes as an `i64` and stores them verbatim (`:54`, `:61`) with no ceiling, no
comparison against `Clock`, and no rejection of a value already in the past. The only
consumer, `is_authorized_signer` (`trading_credit.rs:114-116`), tests `now < session_expiry`
against whatever was stored. A session authorised with `i64::MAX` is a permanent
trading co-signer on that credit, and the on-chain state is indistinguishable from a
24-hour one. `delegate_trading_credit.rs:181` writes the same fields with the same
absence of validation.

**Proof.** The whole write path, with no bound anywhere between parse and store:

```
$ sed -n '43,61p' programs/slipstream/src/instructions/authorize_session.rs
    if data.len() < IX_DATA_LEN { return Err(ProgramError::InvalidInstructionData); }
    if trading_credit_acc.owner() != program_id { return Err(ProgramError::IllegalOwner); }
    let mut session_authority = [0u8; 32];
    session_authority.copy_from_slice(&data[..32]);
    let session_expiry = i64::from_le_bytes(data[32..40].try_into().unwrap());
    let credit = TradingCredit::from_account_info_mut(trading_credit_acc)?;
    if credit.owner != *owner.key() { return Err(SlipstreamError::InvalidAuthority.into()); }
    credit.session_authority = session_authority;
    credit.session_expiry = session_expiry;
-> `Clock` is never fetched in this file; there is no MAX_SESSION_TTL constant anywhere.

$ grep -rn "SESSION_TTL_SECS" frontend/src/hooks/use-session.ts
48:const SESSION_TTL_SECS = 24 * 60 * 60;     <- the 24h wall lives ONLY in the client
```

**Blast radius.** P3 because only the credit's own owner can set it, so nobody is harmed
without their own signature, and the shipped client does pick 24 hours. It matters
because the security argument in `docs/06-session-keys.md` — the document users are
pointed at to justify holding a hot key in the browser — attributes the bound to the
program. A second client, or a modified one, gets an unbounded session with no on-chain
resistance, and the "leaked key blast radius is bounded in time" row of the table at
`docs/06-session-keys.md:96-104` stops being true.

**Suggested remediation.** In `authorize_session` (and the session leg of
`delegate_trading_credit`), fetch `Clock` and reject `session_expiry > now + MAX_SESSION_TTL`
for a program-level constant, e.g. 7 days. Alternatively, amend
`docs/06-session-keys.md:62-76` to say the expiry ceiling is a client policy, not a
program invariant.

## Cross-slice notes

### S1-X01 · crank_twap reads and writes a Market without pinning its canonical PDA

- **Routed to:** S5
- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** S5-C3
- **Evidence:** `programs/slipstream/src/instructions/crank_twap.rs:27`, `programs/slipstream/src/instructions/crank_twap.rs:36`, `programs/slipstream/src/instructions/crank_twap.rs:53`, `programs/slipstream/src/instructions/place_order.rs:104-120`

**What is wrong.** Found while establishing the `S1-03` coverage map. `crank_twap` is the
sole writer of `Market.last_mark_price` (K1's frozen field). It checks
`market_acc.owner() == program_id` and the `DISC_MARKET` byte, then loads the market
mutably at `:53`, but never derives `[SEED_MARKET, market_index]` — the pin
`place_order.rs:104-120` claims "every other instruction that reads a Market" performs.
S5 owns `crank_twap` and the oracle-account-binding class; routing rather than filing.

**Proof.**

```
$ grep -c SEED_MARKET programs/slipstream/src/instructions/crank_twap.rs
0
$ grep -n "market_acc" programs/slipstream/src/instructions/crank_twap.rs
20:        market_acc,        27: if market_acc.owner() != program_id {
36: let market = Market::from_account_info(market_acc)?;
53: let market = Market::from_account_info_mut(market_acc)?;
```

**Blast radius.** Constrained today by the same argument as `S1-03` — `initialize_market`
is the only creator of `DISC_MARKET` accounts and pins the PDA, and `market_count` is 1
on the live deployment. S5 should judge whether the oracle-identity check it pairs with
is sufficient on its own.

**Suggested remediation.** S5's call; the three-line pin from `place_order.rs:114-120`
is the mechanical fix.

### S1-X02 · compute_funding and execute_trigger read a Market without pinning its canonical PDA

- **Routed to:** S3
- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** S3-C12
- **Evidence:** `programs/slipstream/src/instructions/compute_funding.rs:23`, `programs/slipstream/src/instructions/compute_funding.rs:75`, `programs/slipstream/src/instructions/execute_trigger.rs:74`, `programs/slipstream/src/instructions/place_order.rs:104-120`

**What is wrong.** Same coverage map as `S1-03` / `S1-X01`. `compute_funding` loads the
market mutably at `:56` and `:75` and `execute_trigger` reads it at `:74`; neither
derives `SEED_MARKET`. `compute_funding` writes the cumulative funding index, so a
substituted market would be the input to every subsequent funding payment; S3 owns market
parameter sanity and the trigger path.

**Proof.**

```
$ grep -c SEED_MARKET programs/slipstream/src/instructions/compute_funding.rs \
                      programs/slipstream/src/instructions/execute_trigger.rs
programs/slipstream/src/instructions/compute_funding.rs:0
programs/slipstream/src/instructions/execute_trigger.rs:0
```

**Blast radius.** Same transitive constraint as `S1-03`: no forged `DISC_MARKET` account
is constructible, and `market_count == 1` today.

**Suggested remediation.** S3's call; the pin at `place_order.rs:114-120` is the pattern.

### S1-X03 · The "Close legacy credit" button is offered exactly when the instruction behind it cannot succeed

- **Routed to:** S9
- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** S9-C7
- **Evidence:** `frontend/src/hooks/use-session.ts:959-976`, `frontend/src/hooks/use-session.ts:121-123`, `frontend/src/components/trading/session-panel.tsx:312-333`, `programs/slipstream/src/instructions/close_trading_credit.rs:54`

**What is wrong.** The consumer side of `S1-01`. `state.legacyCredit` is set precisely
when the credit *cannot* be decoded at the current 96-byte layout
(`use-session.ts:121-123`), and that is the only condition under which the UI renders the
"Close legacy credit" button (`session-panel.tsx:312-331`). The instruction it sends
rejects every such account at `close_trading_credit.rs:54`. `closeLegacyCredit`
(`use-session.ts:964-976`) pre-checks only `state.legacyDelegated`, never the length, so
the failure surfaces as a raw transaction error. The panel's copy — *"Close it to reclaim
the rent, then start again"* — promises an outcome the program cannot deliver, and says
nothing about the credit balance the account still holds.

**Proof.**

```
$ sed -n '311,331p' frontend/src/components/trading/session-panel.tsx
  <div ...>Legacy trading-credit detected</div>
  {state.legacyDelegated ? ( ...use a fresh wallet... ) : (
     <p>This credit predates the session-keys upgrade. Close it to reclaim
        the rent, then start again to create a session-enabled one.</p>
     <button onClick={closeLegacyCredit}>Close legacy credit</button> )}

$ sed -n '50,56p' programs/slipstream/src/instructions/close_trading_credit.rs
    if trading_credit_acc.owner() != program_id { return Err(ProgramError::IllegalOwner); }
    let credit = TradingCredit::from_account_info(trading_credit_acc)?;   // needs len >= 96
```

Four live devnet accounts are in exactly this state (see `S1-01`), holding 13,313.24 USDC
between them.

**Blast radius.** Four known wallets are shown an action that always fails and a promise
of rent recovery that cannot happen. Filed against S9 because the defect that is fixable
*in the frontend* is the false promise; the program-side cause is `S1-01`.

**Suggested remediation.** Until `S1-01` is fixed on-chain, replace the button in the
non-delegated legacy branch with the same "use a fresh wallet" copy the delegated branch
already shows, and surface the stranded `credit` balance read at the legacy offset.

### S1-X04 · K5's stated mechanism does not match the on-chain state of the stuck credits, and undelegation alone will not unstick the delegated ones

- **Routed to:** S5
- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** S5-C9
- **Evidence:** `programs/slipstream/src/state/trading_credit.rs:53-55`, `programs/slipstream/src/instructions/undelegate_trading_credit.rs:96`, `programs/slipstream/src/instructions/fund_trading_credit.rs:52-57`

**What is wrong.** Spec §6 K5 records the stuck `TradingCredit`s as "L1 reports them
delegated so `fund_trading_credit` refuses". Read-only devnet queries show that is not
the whole state. The four stuck program-owned credits are returned by
`getProgramAccounts` **under the Slipstream program**, so they are not delegated at all;
what refuses them is the 96-byte length guard at `trading_credit.rs:53-55`, one line
*after* the ownership check at `fund_trading_credit.rs:52`. Separately, twelve more
`DISC_TRADING_CREDIT` accounts are delegation-program-owned *and* 56 bytes — for those,
a successful undelegation is necessary but not sufficient: `undelegate_trading_credit.rs:96`
loads the credit through the same guard and will reject it on the way back.

**Proof.** Read-only devnet, `2026-08-21T19:17Z`:

```
getProgramAccounts(7qujfsb4…, dataSize=56)  -> 49 accounts, disc histogram {"3":45,"6":4}
   the 4 disc=6 are PROGRAM-owned 56-byte credits (listed in S1-01), credit>0, not delegated

getProgramAccounts(DELeGGvXpWV2fqJUhqcF5ZSYMS4JTLjteaAMARRSaeSh,
                   dataSize=56, memcmp offset 0 == 0x06)  -> 12 accounts
   e.g. 3kz2Ex2jM1MVjNcHE4gcSX3qCXXEpjkSq9Vto8DX3xac owner=FkB7cLff… credit=8000000000
        6sr6UxmV5S3nUpdpXMc9wrjFecuYAqSLcPDryx7wSZSK owner=CUfBAWVo… credit=5000000000
        3Cs3TJXdtGWiqfNrePdSaF7uTzTLm7xvgkuGuHwbYb6K owner=anshxnbj… credit=0
   -> delegated AND legacy-length: two independent blockers, not one
```

**Blast radius.** S5's K5 root-cause analysis and any unsticking runbook built from it
will be incomplete: fixing the delegation asymmetry recovers nothing for the four
program-owned credits and only half-recovers the twelve delegated ones. Total legacy
credit visible across both sets is well above the 13,313.24 USDC in `S1-01`.

**Suggested remediation.** S5 should treat the layout guard as a second, independent
blocker in its K5 write-up, and sequence any recovery as "length-tolerant load first,
undelegate second".

## Cleared

Negative results. Every threat class in `docs/checks/audit-e2e/classes.tsv` for S1,
including the ones that produced a finding.

- **S1-C1** — Signer checks: all fifteen owned instructions gate on exactly one `is_signer()` and, where state carries an owner/authority, an equality check. Table, rooted at `programs/slipstream/src/instructions/initialize_global.rs:29` and abbreviated to basenames thereafter: `initialize_global.rs:29` (+ none, creation); `initialize_market.rs:42` + `:63`; `initialize_user.rs:24`; `initialize_position.rs:43`; `initialize_trading_credit.rs:27`; `deposit_collateral.rs:31` + `:55`; `withdraw_collateral.rs:42` + `:73`; `fund_trading_credit.rs:28` + `:58`; `withdraw_trading_credit.rs:27` + `:47`; `close_trading_credit.rs:44` + `:55`; `close_user_account.rs:57` + `:73`; `authorize_session.rs:40` + `:57`; `propose_authority.rs:53` + `:72`; `accept_authority.rs:33` + `:52`; `set_market_oracle.rs:34` + `:47`. No bypass found: no owned instruction consults `is_authorized_signer`, and no `owner` field is writable except at init from its own PDA-deriving signer.
- **S1-C2** — Account-ownership validation: every account read as program state in the owned set asserts `owner() == program_id` before the typed load, starting at `programs/slipstream/src/instructions/deposit_collateral.rs:34` — `deposit_collateral.rs:34`/`:51`/`:65`, `withdraw_collateral.rs:51`/`:69`/`:94`, `fund_trading_credit.rs:31`/`:49`/`:52`, `withdraw_trading_credit.rs:38`/`:41`, `close_trading_credit.rs:50`, `close_user_account.rs:60`/`:63`, `authorize_session.rs:48`, `propose_authority.rs:63`, `accept_authority.rs:36`, `set_market_oracle.rs:41`, `initialize_market.rs:55`. The one omission is deliberate and safe: the trailing `Position` accounts at `withdraw_collateral.rs:108-125` and `close_user_account.rs:89-106` carry no owner check, but each is pinned to its canonical PDA first (`withdraw_collateral.rs:114`, `close_user_account.rs:95`), and a Position PDA is only ever created by `initialize_position.rs:71-78` with `owner: program_id`. Clean.
- **S1-C3** — Discriminator validation: every typed load in the owned set checks a `DISC_*` byte, including on the write path — `global_state.rs:27`/`:46`/`:60`, `user_account.rs:26`/`:45`/`:59`, `trading_credit.rs:56`/`:75`/`:89`, with the constants at `state/mod.rs:27-35`. Type confusion between the two 96-byte types (`Position`, `TradingCredit`) is blocked by the write-path check the comment at `trading_credit.rs:71-74` describes. Two raw byte writes bypass the discriminator — `propose_authority.rs:95-96` and `accept_authority.rs:44-57` — but both pin the account to the canonical `SEED_GLOBAL` PDA first (`propose_authority.rs:66-69`, `accept_authority.rs:39-42`), so the account they write is definitionally the GlobalState. No defect.
- **S1-C4** — PDA seed correctness and substitution: covered by finding `S1-03`. Every `find_program_address` call in the owned set uses the canonical (bump-less) derivation and compares the derived key to the supplied account — `initialize_global.rs:39-42`, `initialize_market.rs:58-61`/`:96-110`, `initialize_user.rs:31-37`, `initialize_position.rs:55-61`, `initialize_trading_credit.rs:39-45`, `deposit_collateral.rs:37-40`, `withdraw_collateral.rs:97-100`/`:110-116`/`:127-133`, `fund_trading_credit.rs:34-37`, `close_user_account.rs:66-69`/`:91-97`, `propose_authority.rs:66-69`, `accept_authority.rs:39-42`, `set_market_oracle.rs:55-61`. No instruction accepts a caller-supplied bump anywhere in the owned set; every signer-seed set re-uses the bump `find_program_address` just returned (`initialize_user.rs:45-46`, `withdraw_collateral.rs:135-136`). The gaps are the omitted derivations listed in `S1-03`, not incorrect ones.
- **S1-C5** — Owner-vs-session-key boundary: contract published above under `## Producer contract`; residual gap filed as `S1-07`. The boundary itself holds — `is_authorized_signer` (`programs/slipstream/src/state/trading_credit.rs:110-117`) has exactly two callers, both outside the owned set, and no owned instruction reads `session_authority`.
- **S1-C6** — Vault accounting: deposit and withdraw agree exactly. `deposit_collateral.rs:75-87` transfers `amount` then `checked_add`s the same `amount`; `withdraw_collateral.rs:137-149` transfers `amount` then `checked_sub`s the same `amount`, with gate 3 at `:86-88` guaranteeing the subtraction cannot underflow. No path exists where one succeeds and the other does not: both arms are in the same instruction, so a failing `checked_*` reverts the CPI with it. `amount == 0` is rejected on both sides (`deposit_collateral.rs:47`, `withdraw_collateral.rs:65`). The only disagreement I could construct is the aliasing case filed as `S1-06`.
- **S1-C7** — Account-closing safety: covered by findings `S1-01` and `S1-02`. The parts that are sound: both closers zero the data before draining lamports, so the discriminator is invalidated in the same instruction (`close_trading_credit.rs:64-72`, `close_user_account.rs:109-117`); revival within the same transaction is blocked because `initialize_trading_credit.rs:46-48` and `initialize_user.rs:39-41` test `data_is_empty()`, which is length-based and still false for a zeroed-but-unreaped account; the rent refund goes to the signer that was checked against the account's own `owner` field, and aliasing the refund destination with the drained account is impossible because the drained account is a PDA and cannot sign. `close_user_account`'s open-position gate is mandatory rather than optional-scan (`close_user_account.rs:86-106`), which is the right shape.
- **S1-C8** — `fund_trading_credit` capping: **not clear** — covered by `S5-X04` (P0, routed to this class in `docs/audit/audit-e2e/s5.md`). What is sound is sound only while the account is undelegated: with `TradingCredit` program-owned, `TradingCredit.credit` has exactly three writers in the program — `fund_trading_credit.rs:70-74` (raises; requires `owner.is_signer()` at `:28` and `credit.owner == owner` at `:58`), `withdraw_trading_credit.rs:57-58` (lowers, same gates), and `reconcile_credit` at `programs/slipstream/src/state/trading_credit.rs:137-140`, which only ever `saturating_sub`s. The raise is bounded by `user.free_collateral` (`fund_trading_credit.rs:61-63`) and the debit/credit pair is atomic within one instruction (`:67-74`). That enumeration is over program source, so it is complete for the program and incomplete for the account: `delegate_trading_credit` assigns the whole `TradingCredit` to the delegation program (`programs/slipstream/src/instructions/delegate_trading_credit.rs:227-231`), after which the ER owns every byte of `credit` and writes it back on commit — a fourth writer no owner signature gates. L1 re-validates none of it: `programs/slipstream/src/instructions/withdraw_trading_credit.rs:53-58` checks `credit.credit >= amount` against the field itself, then `:60-64` adds `amount` to `UserAccount.free_collateral`, which `withdraw_collateral` pays out of the vault, and the struct carries no `funded` high-water mark to compare against (`programs/slipstream/src/state/trading_credit.rs:27-46`). `README.md:44-50`'s "capped, not unlimited" claim therefore holds on the L1 side only for a credit that has never been delegated.
- **S1-C9** — Authority rotation: the two-step is correct. Bricking is not reachable — `propose_authority.rs:95-96` will store any 32 bytes, but nothing changes until `accept_authority.rs:52-56` sees a signature from that exact key, so a typoed pubkey costs nothing and can be overwritten by re-proposing. Both legs check what they must: `propose_authority.rs:70-75` requires the *current* `GlobalState.authority` to sign; `accept_authority.rs:33`/`:52` requires the pending key to sign and rejects an all-zero pending, which also gives the current authority an explicit cancel (propose zeros). Accepting clears the pending slot (`accept_authority.rs:57`), so a proposal cannot be replayed after a rotation. The rent top-up before the lazy resize is present and ordered correctly (`propose_authority.rs:80-93`). Residual: a proposal has no expiry, so a forgotten one stays acceptable indefinitely — standard for this pattern and not filed. Live devnet shows the extension has never been created (`GlobalState` is 104 bytes, so `accept_authority` currently returns `InvalidAuthority` at `:47`).
- **S1-C10** — Global pause coverage: covered by finding `S1-04`, which contains the full call-site map. The seven instructions that do call it read the pause from an account pinned to the canonical `SEED_GLOBAL` PDA first, so the gate itself cannot be dodged with a forged `GlobalState` (`programs/slipstream/src/instructions/deposit_collateral.rs:34-41`, `fund_trading_credit.rs:31-38`, `place_order.rs:73-79`).
- **S1-C11** — Instruction-data parsing: clean across the whole owned set. The dispatcher handles an empty buffer via `split_first()` (`programs/slipstream/src/instructions/mod.rs:108-110`). Every owned `process` that reads data checks a length floor before any indexing and never indexes past it: `initialize_global.rs:35` (64, reads `[0..64]`), `initialize_market.rs:67` (37, reads to `data[36]`), `initialize_position.rs:49` (2), `initialize_trading_credit.rs:33` (2), `deposit_collateral.rs:43` (8), `withdraw_collateral.rs:61` (8), `fund_trading_credit.rs:40` (8), `withdraw_trading_credit.rs:30` (8), `authorize_session.rs:43` (40, reads `[0..40]`), `propose_authority.rs:59` (32), `set_market_oracle.rs:37` (64). `initialize_user`, `close_user_account`, `close_trading_credit` and `accept_authority` take no data and ignore the buffer entirely. No unvalidated enum or flag byte exists in the owned set — the only enum-like inputs are `market_index` (any `u16` is legal by design) and the fee fields, which are range-checked at `initialize_market.rs:85-93`. No slice indexing outside a checked window; no panic path found. **`S3-X02` (P2, routed in from S3) is answered on scope, not cleared on merit.** It reports that `funding_interval`, parsed at `initialize_market.rs:76`, is never validated. That is correct and this slice does not dispute it — but it is outside this class: `funding_interval` is a `u64` duration, not an enum or a flag byte, every 8-byte value decodes, and no indexing depends on it, so the parsing claim above is unaffected. What it is missing is a *range bound* — `initialize_market.rs:85-93` rejects a zero `tick_size`, `lot_size` and `max_leverage` and an inconsistent fee pair but never `funding_interval == 0`, which reaches the divisor at `programs/slipstream/src/instructions/compute_funding.rs:41`. That is semantic validation of a market parameter, and the routing slice has already filed it as `S3-07`; it is not re-filed here and this class does not claim it clean.
- **S1-C12** — CPI target validation and duplicate-account aliasing: split verdict. **CPI target: cleared.** The discarded `_token_program` at `deposit_collateral.rs:23` and `withdraw_collateral.rs:34` is harmless because `pinocchio-token` hardcodes the callee — `program_id: &crate::ID` in `/home/anshtyagi/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/pinocchio-token-0.4.0/src/instructions/transfer.rs:54`, with `crate::ID` declared as `TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA` at `/home/anshtyagi/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/pinocchio-token-0.4.0/src/lib.rs:6`; the supplied account is never forwarded. The same holds for every system-program CPI: `pinocchio-system-0.4.0/src/instructions/create_account.rs:79` uses `&crate::ID`, and the owned set additionally pins the passed account by key at `initialize_global.rs:32`, `initialize_market.rs:45`, `initialize_user.rs:27`, `initialize_position.rs:46`, `initialize_trading_credit.rs:30`, `propose_authority.rs:56`. Those six plus the two token transfers are every CPI the owned set makes. Because the callee is Tokenkeg and never Token-2022, a transfer-fee mint cannot be reached at all, so the deposit/withdraw equality of class 6 cannot be broken that way — the mint gap that *does* exist is filed as `S1-05`. **Aliasing: one gap, filed as `S1-06`.** The rest are blocked: same-type program accounts cannot alias because their discriminators differ (`state/mod.rs:27-35`, checked on both read and write paths); `deposit_collateral`'s `user_token_acc`/`quote_vault_acc` pair cannot usefully alias because the SPL authority for a self-transfer would have to be the `vault_authority` PDA rather than the signing `owner`; the two closers cannot alias their drain source with their refund destination because the source is a PDA and the destination must sign; and `withdraw_collateral`/`close_user_account` cannot pass the same `Position` twice because each slot index is checked against its own derived PDA (`withdraw_collateral.rs:108-116`, `close_user_account.rs:89-97`).

## What I did not settle

- `S1-06` rests on SPL Token's `self_transfer` early-return, read from upstream behaviour and not executed here. Settlement criteria are in the record.
- I could not attribute the four stranded owner wallets in `S1-01` to the K5 bot identities: `keepers/.bot-keys/` is gitignored and absent from this checkout. None of the four is the operator key `A5sV4Pkk…`, so at least some are third-party wallets, but the split is unknown.
- No ER validator was available, so anything about how these instructions behave when submitted to `https://devnet.magicblock.app` against a delegated `TradingCredit` is out of reach. In particular I could not test whether `fund_trading_credit` executes inside the ER (where the delegated credit *is* program-owned, so `fund_trading_credit.rs:52` passes) while its `UserAccount` debit lands in a cloned account that never commits back. If the ER permits a write to an undelegated cloned account, that is a value-conservation break; if it rejects the transaction, it is nothing. Settling it needs a signer and an ER, both out of scope for this run.

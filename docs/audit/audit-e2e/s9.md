# S9 — Browser session-key custody and transaction integrity

**Run:** `audit-e2e` · **Freeze:** `b98f0508b01af619f84a6cbb82b6326ebbcd7f9f` · **Issue:** #11

## Calibration

> Flag only gaps that affect correctness, the stated requirements, or documented project invariants -- cite file:line evidence for every finding. Do not report stylistic preferences.

#### Producer contract — `S9-C5`: per-hook poll interval and data-source map (consumed by S13, `S13-C1`)

Derived mechanically by `docs/audit/audit-e2e/repro/s9/poll-budget.mjs` at the freeze SHA.
"Instances" is how many independent copies the default `/trade` render mounts
(`ActivityDrawer` mounts only the `positions` tab, so `use-open-orders.ts` is 0 there).
**No hook in this slice backs off, dedupes, aborts, or guards against re-entry.**

| Hook | Period | Instances | RPC round trips / tick | Where the numbers come from |
|---|---|---|---|---|
| `use-session.ts` | 5000 ms | 3 | 6 | L1 `UserAccount`, wallet USDC ATA, wallet SOL balance, L1 `TradingCredit`, **plus two separate ER reads of the same credit PDA** (`:325-335`, `:361-371`) |
| `use-market.ts` | 5000 ms | 4 | 1 | L1 `MARKET` PDA. `lastMarkPrice` is the frozen K1 value; `twapPrice` is `computeTwap(m)` over the same account |
| `use-orderbook.ts` | 2000 ms | 3 | 2 | ER `ORDER_BOOK` first, **silent fallback to the never-committed L1 copy** (see `S9-02`) |
| `use-open-orders.ts` | 2000 ms | 0 (1 on the Orders tab) | 2 | same book, same fallback; returns a bare array with **no status field at all** |
| `use-er-position.ts` | 2000 ms | 1 | 3 | ER book fill ring + L1 `Market.lastSettledSequence`; the position is a **client-side replay**, never a settled on-chain value |
| `use-positions.ts` | 5000 ms | 1 | 1 | L1 `getProgramAccounts` filtered on the `Position` discriminator. `unrealizedPnl` is computed in the browser from the `markPrice` argument |
| `use-triggers.ts` | 5000 ms | 1 | 1 | L1 `getMultipleAccountsInfo` over the SL/TP trigger PDAs |
| `use-pyth-candles.ts` | 60000 ms | 2 | 1 (`fetch`) | `/api/pyth/history` proxy → Pyth Benchmarks. Real measured candles |
| `use-live-price.ts` | push (~50 ms) | 4 sockets | — | ER Pyth-Lazer feed `ENYwebBThHzmzwPLAQvCucUTsjyfBSZdD9ViXksS4jPu`. **Display only** — no on-chain signature verification, single MagicBlock writer (`use-live-price.ts:19-21`) |
| `use-price-history.ts` | — | **0** | — | Exported but mounted by nothing. `usePriceHistory` / `bucketCandles` are dead |
| `use-positions.ts::useUserAccount` | 5000 ms | **0** | — | Exported but mounted by nothing |

Steady-state total from one idle `/trade` tab: **9.33 RPC round trips per second**, plus 4 WebSockets.

#### Consumed contract — `S1-C5` (owner-vs-session boundary)

S1's findings file had not landed at the time of this audit (`docs/audit/audit-e2e/`
held only other slices' `repro/` directories), so the boundary was **re-derived from
source**: `programs/slipstream/src/instructions/authorize_session.rs:40-42` requires
`owner.is_signer()` and `authorize_session.rs:57-58` requires `credit.owner ==
*owner.key()`; `trading_credit.rs:38-45` scopes the session to one credit and an
expiry. A disagreement between S1's published contract and this re-derivation is
itself worth filing.

---

## Findings

### S9-01 · The withdraw idle-gate blocks the only instruction that can clear the state it blocks on

- **Status:** CONFIRMED
- **Severity:** P1
- **Tag:** [reachable-now]
- **Class:** `S9-C7` — Withdrawal precondition gating
- **Evidence:** `frontend/src/hooks/use-session.ts:786-791`, `frontend/src/hooks/use-session.ts:802`, `programs/slipstream/src/instructions/undelegate_trading_credit.rs:40-48`, `programs/slipstream/src/instructions/undelegate_trading_credit.rs:97`, `tests/unit/src/test_trading_credit.rs:94-96`, `PRODUCT.md:58`

**What is wrong.** `PRODUCT.md:58` requires withdrawal to be gated on an idle account
"with a clear instruction rather than silently failing." `withdraw()` implements that
gate at `use-session.ts:786` by reading `TradingCredit.activeOrders` / `.committed`
and returning early. But the program's own documentation says those two fields go
**stale** exactly when a maker's resting order is filled by someone else's taker: the
maker's credit is not a tx account on that fill, so nothing decrements it
(`trading_credit.rs:20-26`). `undelegate_trading_credit.rs:40-48` states outright that
its `reconcile_credit` call exists so that the stale copy does not "show a stale
nonzero committed/active_orders forever — permanently failing
`withdraw_trading_credit`'s `is_idle()` gate." The client gate sits **16 lines above**
the `createUndelegateTradingCreditInstruction` call (`use-session.ts:802`) and returns
before reaching it, so the user can never run the reconcile that would clear the gate.
The message compounds it: it names only `activeOrders`, so the `committed > 0,
activeOrders == 0` leg — the leg `PRODUCT.md:58` names explicitly as "no committed
margin" — renders as *"Cancel your 0 open orders before withdrawing."*

**Proof.** `docs/audit/audit-e2e/repro/s9/withdraw-gate.mjs` — mechanical assertions
over the frozen tree plus the rendered message text:

```
$ node docs/audit/audit-e2e/repro/s9/withdraw-gate.mjs
use-session.ts withdraw() control flow
  idle gate                     : line 786
  error message                 : line 788
  undelegate instruction built  : line 802
  -> gate precedes undelegate by 16 lines and returns early: CONFIRMED

undelegate_trading_credit.rs
  reconcile_credit call         : line 97
  authors' own rationale        :
    40: /// After this instruction, the TradingCredit is owned by the program again and the
    41: /// user can `withdraw_trading_credit`. `committed`/`active_orders` are reconciled
    42: /// against the (also ER-delegated) order book before the commit below: a maker
    43: /// whose resting order was filled by someone else never gets an ER-side
    44: /// reconcile of their own (that only happens as a side effect of the OWNER's
    45: /// own place_order/cancel_order calls), so without this the committed L1 copy
    46: /// could show a stale nonzero committed/active_orders forever — permanently
    47: /// failing withdraw_trading_credit's `is_idle()` gate for an order that has
    48: /// nothing left reserved.

tests/unit/src/test_trading_credit.rs
  92:     let owner = [7u8; 32];
  93:     // No active slots for this owner (was fully filled and freed)
  94:     let mut credit = make_credit(owner, 1000);
  95:     credit.committed = 500; // stale
  96:     credit.active_orders = 1; // stale

Message the gate renders:
  committed=500, activeOrders=1 (stale, book has 0 of this owner's slots)
    -> "Cancel your 1 open order before withdrawing."   <- useOpenOrders shows 0 orders to cancel
  committed=500, activeOrders=0 (PRODUCT.md's "no committed margin" leg)
    -> "Cancel your 0 open orders before withdrawing."

use-open-orders.ts:68 iterates book.orderSlots, not credit.activeOrders,
so the Open Orders panel shows nothing to cancel while the gate demands a cancel.

ALL ASSERTIONS PASSED
```

The stale state is not hypothetical: `tests/unit/src/test_trading_credit.rs:87-105`
constructs it (`committed = 500` and `active_orders = 1` with **zero active slots for
that owner**) precisely to test that `reconcile_credit` repairs it.

**Blast radius.** Any user who rests a limit order that another account fills. Their
entire `TradingCredit` balance becomes unreachable through the UI: Withdraw refuses,
and the Open Orders panel — which reads `book.orderSlots`, not the credit
(`use-open-orders.ts:68`) — shows nothing to cancel, so the instruction the error gives
cannot be followed. There is an undocumented escape (placing any new order runs
`reconcile_credit` at `place_order.rs:198`), but nothing in the UI says so. On mainnet
this is a user's real margin stranded behind a client-side check; on devnet it is the
same path with worthless tokens. Nothing surfaced it because the devnet book has not
matched a fill since the keepers died (K1/K4), so no maker has been filled in 16 days.

**Suggested remediation.** Move the idle check to *after* the undelegate leg (or drop
it and let `withdraw_trading_credit`'s on-chain `is_idle()` be the gate), since
undelegating is what reconciles. If a pre-check is kept, gate it on the owner's active
slot count read from the order book rather than the credit's stale counter, and give
`committed > 0 && activeOrders == 0` its own message.

---

### S9-02 · The order-book hooks silently fall back to an L1 copy that is documented as never committed, and label it "live"

- **Status:** CONFIRMED
- **Severity:** P1
- **Tag:** [reachable-now]
- **Class:** `S9-C5` — Async correctness
- **Evidence:** `frontend/src/hooks/use-orderbook.ts:84-87`, `frontend/src/hooks/use-orderbook.ts:115`, `frontend/src/hooks/use-orderbook.ts:31-36`, `frontend/src/hooks/use-open-orders.ts:56-59`, `frontend/src/hooks/use-er-position.ts:176-179`, `docs/03-ephemeral-rollups-and-delegation.md:172`

**What is wrong.** All three book-reading hooks try the ER and fall back to the base
layer when the ER read throws. `docs/03-ephemeral-rollups-and-delegation.md:172` states
the 612 KB OrderBook is "delegated once forever — never committed, never undelegated",
so the L1 copy is frozen at its state at delegation time and can never advance. It
still carries a valid discriminator, so `decodeOrderBook` accepts it and
`use-orderbook.ts:115` sets `status: "live"` with `updatedAt: Date.now()`. That directly
contradicts the hook's own docstring at `use-orderbook.ts:31-36`, which says rendering
an ambiguous book state as one thing "is a lie" and is why the status enum exists at
all — the enum has a `"stale"` value but it is only reachable from the `catch`
(`:118-126`), never from the fallback. `use-open-orders.ts:56-59` has no status field
whatsoever: the fallback returns whichever orders were resting at delegation time and
omits the user's real ER orders, indistinguishable from "you have none".
`use-er-position.ts:176-179` replays a fill ring that stopped at sequence 10.

**Proof.** Live read of both layers, `docs/audit/audit-e2e/repro/s9/er-vs-l1-book.sh`
(read-only RPC, no signer):

```
$ bash docs/audit/audit-e2e/repro/s9/er-vs-l1-book.sh
OrderBook 83zMFL6cHjgXkQ7KRNcgtHaZ1fhyNgxhM8aMpPpEnMqe — header read at 2026-08-21T19:24:43Z
L1: owner=DELeGGvXpWV2fqJUhqcF5ZSYMS4JTLjteaAMARRSaeSh
    disc=5 activeOrderCount=11 bidLevelCount=3 askLevelCount=3 fillEventCount=9 nextOrderId=26 nextFillSequence=10
ER: owner=7qujfsb4ZPbQHYVZdqiXq1r8tVAMyyukX94obPqXbVwz
    disc=5 activeOrderCount=12 bidLevelCount=6 askLevelCount=6 fillEventCount=4096 nextOrderId=765434 nextFillSequence=44190
```

The L1 copy the fallback reaches is **44,180 fill sequences and 765,408 order ids
behind** the ER copy, and it decodes cleanly (`disc=5`), so nothing in the hook can tell
the two apart. Address from `frontend/src/lib/deploy-manifest.generated.json`; header
layout from `frontend/src/lib/slipstream/accounts.ts:374-398`.

**Blast radius.** Every viewer of `/trade` during any ER or `/api/rpc/er` proxy
failure — the same same-origin proxy path implicated in K1's quota exhaustion. They see
a depth ladder, a trade tape, an open-orders list and a pending position that are all
presented as current and are in fact a snapshot from delegation. A trader who acts on
that ladder prices against a book that has moved 44k fills. `PRODUCT.md` Principle 1
("every claim on screen traces to something a reader can check") makes this a
correctness defect, not a display one, and the mislabelling is what makes it worse than
showing nothing.

**Suggested remediation.** Do not fall back for the order book: it is the one account
whose base-layer copy is known to be meaningless. Either drop the base-layer branch in
all three hooks and return `status: "unavailable"`, or keep it and mark the result
`"stale"` with the header's `nextFillSequence` as the staleness evidence. Give
`useOpenOrders` the same status enum the other two have.

---

### S9-03 · The terminal issues 9.33 unbounded, undeduplicated RPC round trips per second per tab, with no backoff on failure

- **Status:** CONFIRMED
- **Severity:** P1
- **Tag:** [reachable-now]
- **Class:** `S9-C5` — Async correctness
- **Evidence:** `frontend/src/hooks/use-session.ts:402-406`, `frontend/src/hooks/use-session.ts:325-335`, `frontend/src/hooks/use-session.ts:361-371`, `frontend/src/hooks/use-market.ts:87-91`, `frontend/src/hooks/use-orderbook.ts:129-133`, `frontend/src/hooks/use-er-position.ts:207-211`, `frontend/src/hooks/use-positions.ts:119-123`, `frontend/src/hooks/use-triggers.ts:51-55`

**What is wrong.** Every polled hook is `setInterval(fetch, N)` with a bare `catch {}`
and nothing else: no in-flight guard, no `AbortController`, no backoff, no shared
instance. Three consequences compound. (1) **Duplication** — hooks are called
per-component, so one render mounts 4 `useMarket`, 3 `useSession`, 3 `useOrderBook` and
4 `useLivePrice` WebSockets, each with its own timer over the same account. (2)
**Redundancy within a tick** — `useSession.refresh` reads the *same* ER credit PDA twice
per tick, from two freshly constructed `Connection` objects (`:325-335` and `:361-371`).
(3) **No back-pressure** — a failing or rate-limited RPC is swallowed and the next tick
fires on the same period, so slow responses overlap and the request rate rises exactly
when the endpoint is least able to serve it. The spec's own `S9-C5` names "unbounded
polling that contributes to K1's quota problem"; K1's root-cause chain is public devnet
RPC quota exhaustion, and the browser reaches that same endpoint through
`/api/rpc/base`. I do **not** claim the frontend caused K1 — I have no per-client
attribution — only that its request rate is unbounded by construction and measured
below.

**Proof.** `docs/audit/audit-e2e/repro/s9/poll-budget.mjs` derives the period, the
per-tick RPC count and the mount count from source, and asserts the absence of every
back-pressure mechanism:

```
$ node docs/audit/audit-e2e/repro/s9/poll-budget.mjs
hook                  period  rpc/tick  instances  req/s  mounted in
------------------------------------------------------------------------------------------------
use-session.ts        5000ms         6          3   3.60  order-form.tsx, session-panel.tsx, positions-table.tsx
                      calls: getAccountInfo, getAccount, getBalance, getAccountInfo, getAccountInfo, getAccountInfo
use-market.ts         5000ms         1          4   0.80  dashboard.tsx, market-bar.tsx, order-form.tsx, status-panel.tsx
                      calls: getAccountInfo
use-orderbook.ts      2000ms         2          3   3.00  order-book-display.tsx, status-panel.tsx, fill-toasts.tsx
                      calls: getAccountInfo, getAccountInfo
use-positions.ts      5000ms         1          1   0.20  positions-table.tsx
                      calls: getProgramAccounts
use-er-position.ts    2000ms         3          1   1.50  positions-table.tsx
                      calls: getAccountInfo, getAccountInfo, getAccountInfo
use-triggers.ts       5000ms         1          1   0.20  positions-table.tsx
                      calls: getMultipleAccountsInfo
use-open-orders.ts    2000ms         2          0   0.00  
                      calls: getAccountInfo, getAccountInfo
use-pyth-candles.ts   60000ms         1          2   0.03  market-bar.tsx, price-chart.tsx
                      calls: fetch
------------------------------------------------------------------------------------------------
steady-state RPC round trips from one idle /trade tab: 9.33 per second
plus 4 independent accountSubscribe WebSockets (use-live-price.ts), one per useLivePrice() call

No hook contains AbortController / in-flight guard / backoff / retryAfter:
every failure is swallowed by a bare catch and the next tick fires on the same period.

ALL ASSERTIONS PASSED
```

Note `use-positions.ts` contributes a `getProgramAccounts` scan every 5 s per tab — the
single most expensive call in the set — and `use-open-orders.ts` adds a further 1.00
req/s whenever the Orders tab is selected.

**Blast radius.** The whole deployment, and anything else sharing the endpoint. Ten
concurrent viewers is ~93 requests/second against `api.devnet.solana.com` plus 40 ER
sockets, with the rate rising rather than falling as the endpoint degrades. The keepers
share that endpoint and died of quota exhaustion. On mainnet this is a per-viewer RPC
bill that scales with tab count rather than with information, and a self-reinforcing
outage under any provider throttle.

**Suggested remediation.** Hoist each hook's state into one provider/store per account
so N components share one poller; add an in-flight guard plus exponential backoff on
error to every interval; drop the duplicated ER read in `refresh` and reuse one ER
`Connection` module-wide, as `use-wallet-compat.ts:30-32` already does for the base
layer.

---

### S9-04 · The session key's 64-byte secret is persisted to `localStorage` and never cleared, while the docs say it is held in memory

- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** `S9-C1` — Session-key custody
- **Evidence:** `frontend/src/hooks/use-session.ts:158-173`, `frontend/src/hooks/use-session.ts:148-151`, `docs/06-session-keys.md:111`, `README.md:229`

**What is wrong.** `docs/06-session-keys.md:111` states the frontend "keeps the session
key **in memory** to auto-sign ER orders." It does not: `storeSession` writes
`Array.from(keypair.secretKey)` — the full 64-byte ed25519 secret — as a JSON array
into `window.localStorage` under `slipstream:session:<owner>:<market>`
(`use-session.ts:138-140`, `:165-172`). Nothing ever removes it. A repo-wide grep finds
no `removeItem`, no clear-on-disconnect, and no clear-on-expiry: an expired key's secret
bytes stay on disk indefinitely, and switching or disconnecting the wallet leaves the
previous owner's key in place. The written invariant that *is* upheld —
`README.md:229`, "the session key can only place and cancel orders" — holds (see
`## Cleared`, `S9-C1`), which is why this is P2 and not higher: the leak radius is order
churn inside one credit, not the vault.

**Proof.**

```
$ grep -rn "secretKey\|localStorage" frontend/src/hooks/use-session.ts
frontend/src/hooks/use-session.ts:130:// Ephemeral session-key storage (localStorage, per owner+market).
frontend/src/hooks/use-session.ts:134:  secretKey: number[]; // 64-byte ed25519 secret
frontend/src/hooks/use-session.ts:148:    const raw = window.localStorage.getItem(sessionStorageKey(owner, marketIndex));
frontend/src/hooks/use-session.ts:151:    const keypair = Keypair.fromSecretKey(Uint8Array.from(parsed.secretKey));
frontend/src/hooks/use-session.ts:166:    secretKey: Array.from(keypair.secretKey),
frontend/src/hooks/use-session.ts:169:  window.localStorage.setItem(

$ grep -rn "removeItem\|clearSession\|revoke" frontend/src
frontend/src/content/docs/06-session-keys.md:42:3. **Expire / revoke.** Once `now >= session_expiry`, the key is automatically

$ grep -n "in memory to auto-sign" docs/06-session-keys.md
111:`authorize_session`, then keeps the session key in memory to auto-sign ER orders.
```

The only hit for revocation is prose in a doc; there is no code path that clears the
stored secret.

**Blast radius.** Anyone with read access to the browser profile — a shared or kiosk
machine, a synced profile, a malicious extension with storage permission, or any script
that runs on the origin (see `S9-05`) — recovers a live session key for up to
`SESSION_TTL_SECS` = 24 h (`use-session.ts:48`). With it they can place and cancel
orders against the victim's `TradingCredit` until expiry, burning the committed margin
by crossing the spread. `docs/06-session-keys.md:102-105` already bounds this as
"annoying order churn inside one market's pre-committed margin" — the finding is that
the stated custody model ("in memory") is strictly stronger than the implemented one,
so that bound is being reasoned about against the wrong premise.

**Suggested remediation.** Either hold the keypair in a module-level ref (matching the
doc) and accept a re-authorize on reload, or keep persistence and add `removeItem` on
wallet disconnect, on owner change, and when `loadStoredSession` observes an expired
entry. Then correct `docs/06-session-keys.md:111` to whichever is chosen (routed as
`S9-X03`).

---

### S9-05 · The docs renderer pipes unsanitized `marked` output into `dangerouslySetInnerHTML` on the origin that holds the session secret, with no CSP

- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** `S9-C2` — XSS reachability of the session key
- **Evidence:** `frontend/src/lib/docs.ts:97`, `frontend/src/app/docs/page.tsx:12`, `frontend/src/app/docs/[slug]/page.tsx:24`, `frontend/src/app/docs/mermaid-runner.tsx:26-35`, `frontend/src/hooks/use-session.ts:169`, `frontend/next.config.ts:3-5`

**What is wrong.** `getDoc` renders markdown with `marked.parse` and returns the raw
string (`frontend/src/lib/docs.ts:97`); both doc routes inject it with
`dangerouslySetInnerHTML` (`frontend/src/app/docs/page.tsx:12`,
`frontend/src/app/docs/[slug]/page.tsx:24`). `marked` v14 does not sanitize — the
`sanitize` option was removed in v5 and the project ships no sanitizer, so raw HTML in a
source markdown file reaches the DOM verbatim. The one hand-escaped path is no
exception: `frontend/src/lib/docs.ts:89-92` escapes `&`, `<`, `>` into the
`<pre class="mermaid">` block, but `frontend/src/app/docs/mermaid-runner.tsx:33` stores
the *decoded* `el.textContent` into `data-src` and `:30` writes it back with
`el.innerHTML = src` on the next render pass — which the `themechange` listener at `:59`
fires — so that escaping survives only the first render. That is all defensible on its
own because the input is build-time repo content copied by
`frontend/scripts/copy-docs.mjs`, not user input. What makes it a finding is the
*composition*: this renderer shares an origin with a plaintext ed25519 secret in
`localStorage` (`frontend/src/hooks/use-session.ts:169`), and there is no
Content-Security-Policy anywhere in the deployment to contain a script if one ever does
execute. The answer to `S9-C2`'s question — "if a script runs on the origin, does it get
the key?" — is unambiguously yes, with no second line of defence.

**Proof.**

```
$ grep -rniE "dompurify|sanitize|Content-Security-Policy" frontend/src frontend/next.config.ts frontend/vercel.json frontend/package.json; echo "exit=$?"
exit=1

$ grep -rn "dangerouslySetInnerHTML" frontend/src
frontend/src/app/layout.tsx:60:          dangerouslySetInnerHTML={{
frontend/src/app/docs/[slug]/page.tsx:24:        dangerouslySetInnerHTML={{ __html: doc.html }}
frontend/src/app/docs/page.tsx:12:        dangerouslySetInnerHTML={{ __html: doc.html }}
```

Zero matches for any sanitizer or any CSP declaration across the frontend source, the
Next config, the Vercel config and the manifest. `frontend/next.config.ts:3-5` is an
empty config object with no `headers()`.

The mermaid path looks hardened and is not. `frontend/src/lib/docs.ts:89-92` hand-escapes
`&`, `<`, `>` before building the `<pre class="mermaid">` block, but the runner decodes it
again on every re-render:

```
$ sed -n '26,35p' frontend/src/app/docs/mermaid-runner.tsx
      // Restore raw graph source if a previous run already replaced it with SVG.
      for (const el of nodes) {
        const src = el.getAttribute("data-src");
        if (src) {
          el.innerHTML = src;
          el.removeAttribute("data-processed");
        } else {
          el.setAttribute("data-src", el.textContent || "");
        }
      }
```

`el.textContent` at `:33` is the decoded text, so the `&amp;`/`&lt;`/`&gt;` produced at
`frontend/src/lib/docs.ts:89-92` is stored raw in `data-src` and put straight back through
`innerHTML` at `:30` on the next pass — and the `themechange` listener at
`frontend/src/app/docs/mermaid-runner.tsx:59` fires one. `securityLevel: "strict"`
(`frontend/src/app/docs/mermaid-runner.tsx:39`) governs mermaid's own label rendering and
runs after `:30`, so it does not cover this. Both routes into the docs DOM are therefore
unescaped; the provenance of the markdown, not the escaping, is what bounds them.

**Blast radius.** P2 today because no attacker-controlled input reaches `marked`: the
only sources are the ten files under `frontend/src/content/docs/`, and `slugToFile`
matches the request slug against `readdirSync` basenames rather than joining it into a
path (`frontend/src/lib/docs.ts:51-58`), so there is no traversal. It becomes P1 the moment any
user-supplied or externally-fetched markdown is rendered through the same helper, or if
a third-party script on the page is compromised — the spec's own `S9-C2` text calls a
stored session key plus a docs-renderer XSS "a P1 chain even on devnet."

**Suggested remediation.** Sanitize `getDoc`'s output (DOMPurify or `marked`'s
`walkTokens` with an allowlist) so the pipeline is safe by construction rather than by
provenance, and add a Content-Security-Policy via `next.config.ts`'s `headers()` — a
`script-src` without `unsafe-inline` would also need `frontend/src/app/layout.tsx:60`'s
inline theme script nonced. The mermaid half is separate: stop
`frontend/src/app/docs/mermaid-runner.tsx:26-35` re-decoding the escaped source — cache the
escaped markup, or restore it with `textContent` instead of `innerHTML`. The CSP half is routed to S10 as `S9-X01`.

---

### S9-06 · `getSessionKeypair`'s contract claims an on-chain authority check it does not perform

- **Status:** CONFIRMED
- **Severity:** P3
- **Class:** `S9-C1` — Session-key custody
- **Evidence:** `frontend/src/hooks/use-session.ts:230-243`, `frontend/src/hooks/use-session.ts:375-378`, `frontend/src/components/trading/order-form.tsx:153-155`

**What is wrong.** The docstring at `use-session.ts:231-235` states the function returns
the keypair "IFF it is still valid: it exists, has not expired locally, **and matches
the session authority currently stored on-chain**." The body (`:236-243`) checks only
existence and the local expiry timestamp; it never reads `credit.sessionAuthority`. The
on-chain comparison actually lives in `refresh()` (`:375-378`) and is exposed as
`state.sessionActive`, and correctness depends on all three call sites remembering to
combine them — `const useSessionKey = session.sessionActive && sessionKp !== null`. All
three currently do, so nothing is broken today; the defect is that a documented guard is
not where it says it is, so a fourth call site that trusts the docstring gets an
unvalidated key.

**Proof.**

```
$ sed -n '236,243p' frontend/src/hooks/use-session.ts
  const getSessionKeypair = useCallback((): Keypair | null => {
    if (!publicKey) return null;
    const stored = loadStoredSession(publicKey, marketIndex);
    if (!stored) return null;
    const nowSecs = Math.floor(Date.now() / 1000);
    if (stored.expiry <= nowSecs) return null;
    return stored.keypair;
  }, [publicKey, marketIndex]);

$ grep -rn "getSessionKeypair()" frontend/src
frontend/src/components/trading/open-orders.tsx:29:      const sessionKp = getSessionKeypair();
frontend/src/components/trading/order-form.tsx:153:      const sessionKp = getSessionKeypair();
frontend/src/components/trading/positions-table.tsx:76:      const sessionKp = getSessionKeypair();

$ grep -rn "session.sessionActive && sessionKp !== null" frontend/src
frontend/src/components/trading/order-form.tsx:154:      const useSessionKey = session.sessionActive && sessionKp !== null;
frontend/src/components/trading/open-orders.tsx:30:      const useSessionKey = session.sessionActive && sessionKp !== null;
frontend/src/components/trading/positions-table.tsx:77:      const useSessionKey = session.sessionActive && sessionKp !== null;
```

Three call sites, three matching guards — the invariant holds by convention at every
caller, not by construction in the function that documents it.

**Blast radius.** No user harm today. The risk is a future call site (or a refactor that
drops the `sessionActive` conjunct) signing with a key the chain no longer authorizes;
that transaction fails on-chain at `is_authorized_signer`, so the worst case is a
confusing rejected order rather than a loss — which is why this is P3 and not P2.

**Suggested remediation.** Either fold the `sessionActive` check into
`getSessionKeypair` so the function honours its own contract, or delete the third clause
from the docstring and rename the function to signal that the caller must still gate on
`state.sessionActive`.

---

### S9-07 · `waitForUndelegation` charges RPC failures against its retry budget and reports them as the rollup withholding funds

- **Status:** CONFIRMED
- **Severity:** P3
- **Class:** `S9-C6` — Undelegation polling
- **Evidence:** `frontend/src/hooks/use-session.ts:184-200`, `frontend/src/hooks/use-session.ts:815-821`, `PRODUCT.md:43`

**What is wrong.** `PRODUCT.md:43` requires the UI to poll for the asynchronous
ownership flip rather than assume it, and `waitForUndelegation` does exactly that —
30 attempts, 2 s apart, checking `info.owner == PROGRAM_ID` on the base layer. The gap
is in how it fails. Its `catch` at `:194-196` swallows every RPC error and lets the loop
continue, so a transient outage consumes tries from the same 30-attempt budget as a slow
rollup. Thirty consecutive RPC failures therefore exhaust the budget in ~60 s and the
caller reports *"The rollup hasn't handed your funds back yet"* (`:817-819`) — blaming
MagicBlock for what was a devnet RPC problem. Under K1's exact live condition
(quota-exhausted public devnet RPC) that is the misattribution the user will actually
see. The function also cannot distinguish "still settling" from K5's permanent
half-delegated deadlock; both produce the same "give it a moment and press Withdraw
again", which is advice that can never succeed in the K5 case.

**Proof.**

```
$ sed -n '190,199p' frontend/src/hooks/use-session.ts
  for (let i = 0; i < tries; i++) {
    try {
      const info = await conn.getAccountInfo(creditPda, "confirmed");
      if (info && info.owner.toBase58() === programId) return true;
    } catch {
      /* transient RPC error — keep polling */
    }
    await new Promise((r) => setTimeout(r, 2_000));
  }
  return false;
```

The `catch` records nothing; `i` advances identically for a successful "not yet" and for
a thrown request. `waitForUndelegation` returns a bare `boolean`
(`use-session.ts:187-188`), so the caller has no channel to distinguish the two even if
it wanted to.

**Blast radius.** A user mid-withdrawal during any RPC blip is told the rollup is
holding their funds. Funds are not at risk — the undelegate has already been scheduled
on-chain and a later retry succeeds — but the message points the user (and any support
process) at the wrong system, and in the K5 deadlock it advises an action that can never
work. P3 rather than P1 because the withdrawal path itself still completes on retry.

**Suggested remediation.** Count consecutive RPC errors separately from "not flipped
yet", and return a discriminated result (`"flipped" | "timeout" | "rpc-unavailable"`) so
`withdraw()` can say which happened. Cap total wall-clock time instead of attempt count,
and after a timeout report the credit's current L1 owner so a K5 deadlock is
distinguishable from a slow flip.

---

### S9-08 · `humanizeError`'s `/0x1/` pattern matches every Slipstream program error, so any of them is reported as a USDC balance shortfall

- **Status:** CONFIRMED
- **Severity:** P3
- **Class:** `S9-C4` — Transaction construction and signing UX
- **Evidence:** `frontend/src/hooks/use-session.ts:87-89`, `programs/slipstream/src/error.rs:5`, `frontend/src/hooks/use-session.ts:76-97`

**What is wrong.** `humanizeError` classifies a failure by regex over the error message
plus the transaction logs. The third rule, `if (/0x1|insufficient/i.test(hay) &&
/transfer/i.test(hay))`, returns *"USDC transfer failed — your balance is lower than the
deposit amount."* `SlipstreamError` starts at `0x100` (`error.rs:5`) and has 56 sequential
variants, so every custom error the program can return renders as `0x100`…`0x137` — all
of which contain the substring `0x1`. Every transaction that `use-session.ts` builds for
`initialize` or `withdraw` includes an SPL token transfer, so `Instruction: Transfer`
is always in the logs and the second conjunct is always true. The result: **every**
program-level rejection on the money path is reported to the user as a balance
shortfall.

**Proof.**

```
$ V=$(awk '/^pub enum SlipstreamError/{f=1;next} /^}/{f=0} f' programs/slipstream/src/error.rs | grep -cE '^\s+[A-Z][A-Za-z0-9]*\s*(=|,)'); echo "variants: $V"
variants: 56
$ python3 -c "print('first = 0x%X, last = 0x%X' % (0x100, 0x100+56-1)); print('all render with the prefix 0x1:', all(('0x%x'%c).startswith('0x1') for c in range(0x100,0x100+56)))"
first = 0x100, last = 0x137
all render with the prefix 0x1: True

$ node -e '...replay of the use-session.ts:87 predicate against real error strings...'
AccountAlreadyInitialized  custom program error: 0x125  -> humanizeError says: "USDC transfer failed - your balance is lower than the deposit amount."
CreditStillActive          custom program error: 0x129  -> humanizeError says: "USDC transfer failed - your balance is lower than the deposit amount."
GlobalPaused               custom program error: 0x132  -> humanizeError says: "USDC transfer failed - your balance is lower than the deposit amount."
InvalidVault               custom program error: 0x128  -> humanizeError says: "USDC transfer failed - your balance is lower than the deposit amount."
```

(The two earlier rules at `:81` and `:84` intercept genuine lamport/ATA problems first,
so the rule only ever fires on cases it then misdescribes.)

**Blast radius.** Every user who hits any program rejection during deposit, funding or
withdrawal. Most damaging in combination with `S9-01`: a `CreditStillActive` on withdraw
tells the user their USDC balance is too low, steering them away from the actual stale
`committed` condition. No funds move incorrectly; the harm is that the UI's only
diagnostic actively misdirects. P3 rather than P1 because no on-chain state or displayed
figure is wrong, only the explanation.

**Suggested remediation.** Anchor the pattern to a whole error code
(`/custom program error: (0x[0-9a-f]+)/`) and map the captured code through the
`SlipstreamError` enum, falling through to the raw message for anything unmapped;
reserve the balance-shortfall wording for the SPL token error `0x1` alone.

---

## Cross-slice notes

### S9-X01 · No Content-Security-Policy or security headers at the deploy boundary

- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** `S10-C10`
- **Routed to:** S10
- **Evidence:** `frontend/next.config.ts:3-5`, `frontend/vercel.json:1-13`, `frontend/src/hooks/use-session.ts:169`

**What is wrong.** Neither `next.config.ts` (an empty config object, no `headers()`) nor
`vercel.json` declares a Content-Security-Policy, `X-Frame-Options`,
`Referrer-Policy` or `X-Content-Type-Options`. Both files are S10's under spec rule R1,
so S9 files it here rather than as its own finding. S9's stake in it: the origin these
headers would protect stores a live 64-byte ed25519 session secret in `localStorage`
(`use-session.ts:169`), so the CSP is the only thing that would stand between an
injected script and a usable trading key.

**Proof.**

```
$ grep -rniE "dompurify|sanitize|Content-Security-Policy" frontend/src frontend/next.config.ts frontend/vercel.json frontend/package.json; echo "exit=$?"
exit=1
$ cat frontend/next.config.ts
import type { NextConfig } from "next";
const nextConfig: NextConfig = {
  /* config options here */
};
export default nextConfig;
```

**Blast radius.** Any script-injection path on the origin — today only a compromised
dependency or a future user-content renderer — reads the session key with nothing to
stop it, and the app can be framed for clickjacking of the wallet-connect flow.

**Suggested remediation.** Add a `headers()` block in `next.config.ts` with a
`script-src` CSP (nonce the inline theme script at `layout.tsx:60`),
`frame-ancestors 'none'`, `X-Content-Type-Options: nosniff` and a
`Referrer-Policy`.

### S9-X02 · The one-click close builds its limit price from the frozen `last_mark_price`

- **Status:** CONFIRMED
- **Severity:** P1
- **Tag:** [reachable-now]
- **Class:** `S13-C1`
- **Routed to:** S13
- **Evidence:** `frontend/src/components/trading/positions-table.tsx:82-84`, `frontend/src/hooks/use-market.ts:67`

**What is wrong.** `positions-table.tsx:82-84` derives the IOC limit price for closing an
ER position as `mark * 0.95` (closing a long) or `mark * 1.05` (closing a short), where
`mark` is `market.lastMarkPrice` — the value K1 establishes as frozen at $74.114 since
2026-08-05 while the market's own Pyth feed reads $91.85. `use-market.ts:67` reports that
field faithfully; the defect is using it as a live price bound in a transaction. Filed
here because `positions-table.tsx` is S13's file.

**Proof.**

```
mark (frozen, K1)                = 74.114
close a SHORT -> IOC BUY cap     = 74.114 * 1.05 = 77.82
true SOL/USD (K1, Pyth feed)     = 91.85
=> the buy cap sits $14.03 below the market: the IOC cannot fill and expires silently.
close a LONG  -> IOC SELL floor  = 74.114 * 0.95 = 70.41
=> fills, but at a floor set 23% below the true price.
```

**Blast radius.** Anyone using "close position" while the mark is stale: closing a short
silently no-ops (an IOC that cannot cross), and closing a long is priced against a
23%-stale reference. `PRODUCT.md` Principle 1 makes a transaction built from a number
the UI knows is stale a correctness defect.

**Suggested remediation.** Bound the close against the live feed
(`useLivePrice`) or the ER book's own best bid/ask, and refuse to build the order at all
when the mark's age exceeds a threshold — surfacing the staleness rather than pricing
through it.

### S9-X03 · `docs/06-session-keys.md` says the session key is kept in memory; it is written to `localStorage`

- **Status:** CONFIRMED
- **Severity:** P2
- **Class:** `S12-C3`
- **Routed to:** S12
- **Evidence:** `docs/06-session-keys.md:111`, `frontend/src/hooks/use-session.ts:169`, `frontend/src/content/docs/06-session-keys.md:111`

**What is wrong.** The doc half of `S9-04`. `docs/06-session-keys.md:111` — and its
build-time copy at `frontend/src/content/docs/06-session-keys.md:111` — states the
browser "keeps the session key in memory to auto-sign ER orders." The implementation
persists the raw 64-byte secret to `localStorage`. Section 6.4's leak-radius table is
reasoned from the in-memory premise, so the doc understates the exposure window
(a closed tab does not end the session).

**Proof.**

```
$ grep -n "in memory to auto-sign" docs/06-session-keys.md frontend/src/content/docs/06-session-keys.md
docs/06-session-keys.md:111:`authorize_session`, then keeps the session key in memory to auto-sign ER orders.
frontend/src/content/docs/06-session-keys.md:111:`authorize_session`, then keeps the session key in memory to auto-sign ER orders.
$ sed -n '169,172p' frontend/src/hooks/use-session.ts
  window.localStorage.setItem(
    sessionStorageKey(owner, marketIndex),
    JSON.stringify(payload)
  );
```

**Blast radius.** Readers — including future maintainers reasoning about the leak
radius — believe the key dies with the tab. It survives for 24 h on disk.

**Suggested remediation.** Change line 111 to describe `localStorage` persistence keyed
by owner and market with the 24 h TTL, and state explicitly that closing the tab does not
end the session. If `S9-04` is fixed by moving the key into memory instead, the doc is
already correct and only the code changes.

---

## Cleared

Negative results, one bullet per threat class. Each names what was checked and cites a
line a reader can open.

- **`S9-C1`** — Session-key custody. Partially covered by `S9-04` (localStorage
  persistence, no revocation) and `S9-06` (docstring/behaviour split). **Sound:** the
  `README.md:213` claim holds. Every call site that reaches an instruction builder with
  the session keypair was enumerated — `grep -rn "getSessionKeypair()" frontend/src`
  returns exactly three — and all three build only `createPlaceOrderInstruction` or
  `createCancelOrderInstruction`, with the owner passed as the attribution argument and
  the session key only as `signerPk`: `frontend/src/components/trading/order-form.tsx:157-170`,
  `frontend/src/components/trading/open-orders.tsx:33-39`,
  `frontend/src/components/trading/positions-table.tsx:86-100`. No deposit, withdraw,
  fund, delegate, undelegate, authorize or close instruction is ever signed by it — those
  all route through `sendTransaction` (the wallet) in `use-session.ts:501`, `:597`,
  `:666`, `:812`, `:835`, `:865`, `:945`, `:971`. Generation is `Keypair.generate()` in
  the browser (`use-session.ts:640`, `:908`); it is never transmitted (the only network
  send of session material is the **public** key inside the on-chain instruction,
  `use-session.ts:648`); it is scoped per owner and market by the storage key
  (`use-session.ts:138-140`); TTL is 24 h (`use-session.ts:48`); and it is revocable in
  one owner signature via `rotate()` (`use-session.ts:904-955`), which persists the new
  key only after the on-chain `authorize_session` confirms (`use-session.ts:949-950`) —
  the same "persist only after confirm" ordering `delegate()` uses
  (`use-session.ts:671-672`), so a failed transaction cannot orphan a stored key.
- **S9-C2** — XSS reachability of the session key: **not clear** — covered by `S9-05`,
  which owns the live sink. The mermaid path is not the exception it was read as:
  `frontend/src/lib/docs.ts:89-92` hand-escapes `&`, `<`, `>` into the
  `<pre class="mermaid">` block, but `frontend/src/app/docs/mermaid-runner.tsx:33` stores
  the decoded `el.textContent` into `data-src` and `:30` writes it back with
  `el.innerHTML = src` on the next render pass, which the `themechange` listener at `:59`
  fires; `securityLevel: "strict"` (`mermaid-runner.tsx:39`) governs mermaid's own label
  rendering and runs after `:30`. Three named sinks are absent — no `eval`, no
  `new Function`, no `srcdoc` anywhere in `frontend/src` — which rules out those three,
  not the class. What is sound: the slug is not a path:
  `frontend/src/lib/docs.ts:51-58` matches it against `readdirSync` basenames instead of
  joining it, so `../` traversal cannot select a file, and a missing content directory
  degrades to `notFound()` rather than throwing (`docs.ts:36`, `docs.ts:78-79`).
  `frontend/src/app/docs/docs-shell.tsx:65-78` renders nav titles as JSX text, not HTML.
- **`S9-C3`** — The auth callback. **Clean, no finding.**
  `frontend/src/app/auth/callback/page.tsx:13-21` is 22 lines that render Phantom's
  `ConnectBox` and nothing else: it reads no `searchParams`, no `location.hash`, no
  token, and performs no navigation, so there is no open-redirect and no parameter
  handling to get wrong. The redirect URL is constructed once at provider level from
  `window.location.origin` (`frontend/src/components/wallet/wallet-provider.tsx:33-37`),
  never from request input, and the file's own comment records that it must be
  allowlisted in the Phantom Portal or the redirect is rejected upstream
  (`auth/callback/page.tsx:5-7`). Origin validation of the OAuth handoff is therefore
  entirely inside `@phantom/react-sdk`; that dependency's internals were **not** audited
  (`frontend/node_modules/` is absent at the freeze SHA) — settling it needs
  `npm ci` and a read of `ConnectBox`. `wallet-provider.tsx:41-43` degrades to the
  `injected` provider when no `appId` is set, so the social path is off rather than
  half-configured.
- **`S9-C4`** — Transaction construction and signing UX. Covered by `S9-08`. **Sound:**
  `frontend/src/lib/confirm.ts:35-52` polls `getSignatureStatuses`, throws on `st.err`
  with the decoded error attached, and enforces a wall-clock deadline — it cannot report
  a failed transaction as confirmed. Amount handling is checked against chain state, not
  React state, before every money instruction: `use-session.ts:577-588` re-reads
  `freeCollateral` before `fund`, `use-session.ts:427-446` re-reads the ATA balance before
  `initialize`, and both refuse with a specific message rather than letting the wallet
  simulate a doomed transaction. `frontend/src/hooks/use-wallet-compat.ts:89-98` fills
  `feePayer`/`recentBlockhash` only when unset, so the ER-blockhash transactions built at
  `use-session.ts:809-811` and `:939-941` keep their own values. No blind-signing
  surface was found in the owned set: the session key signs only the two ER instructions
  enumerated under `S9-C1`, and every wallet-signed transaction is built from constants
  or from freshly read chain state. Presentation of these amounts is S13's half.
- **`S9-C5`** — Async correctness. Covered by `S9-02` and `S9-03`; the poll-interval and
  data-source map (`frontend/src/hooks/use-market.ts:87-91`) is published above as the
  S13 producer contract. **Sound:** unmount cleanup is present in every polled hook —
  `clearInterval` in the effect's return at
  `frontend/src/hooks/use-market.ts:90`, `use-orderbook.ts:132`, `use-open-orders.ts:93`,
  `use-er-position.ts:210`, `use-positions.ts:122`, `use-triggers.ts:54`,
  `use-session.ts:405`, `use-pyth-candles.ts:85` — and
  `frontend/src/hooks/use-live-price.ts:127-132` additionally sets a `cancelled` flag,
  clears the pending reconnect timer and closes the socket, so no listener survives
  unmount. `frontend/src/hooks/use-pyth-candles.ts:42-56` is the one hook that guards
  against out-of-order responses, via a `reqId` ref checked before every `setState`. No
  stale-closure defect was found: every polled callback is a `useCallback` whose
  dependency list contains each captured value it reads.
- **`S9-C6`** — Undelegation polling. Covered by `S9-07`. **Sound:** the requirement in
  `PRODUCT.md:43` that the UI poll rather than assume is met —
  `frontend/src/hooks/use-session.ts:184-200` polls the **base layer** for the ownership
  flip back to `PROGRAM_ID` after the ER-side schedule, and
  `use-session.ts:816-821` handles the poll never succeeding by returning `false` with a
  user-facing message rather than proceeding to `withdraw_trading_credit` and failing
  with `CreditStillActive`. The ordering is right: undelegate is only attempted when the
  L1 owner is the delegation program (`use-session.ts:769`, `:797`), and each leg of
  `withdraw` is skipped when already done, so a retry resumes rather than repeating.
- **S9-C7** — Withdrawal precondition gating. Covered by `S9-01`; the `closeLegacyCredit`
  half is **not clear** — see `S1-X03`, routed in from S1. **Sound in the parts not filed:**
  the predicate at `frontend/src/hooks/use-session.ts:786` does test both
  legs `PRODUCT.md:58` names (`activeOrders > 0 || committed > 0n`), matching the
  program's own `is_idle()` at
  `programs/slipstream/src/state/trading_credit.rs:101`, and it prefers the ER copy of
  the credit over the base-layer copy when delegated (`use-session.ts:776-783`) so it
  reads the authoritative state where one exists. **Withdrawn:** the claim that
  `closeLegacyCredit` (`use-session.ts:961-977`) mirrors the program-side guard. It mirrors
  one of two guards — the delegation check at `use-session.ts:963-967` — and not the length
  floor at `programs/slipstream/src/state/trading_credit.rs:53-55`, which per `S1-01`
  refuses every 56-byte credit whether delegated or not
  (`programs/slipstream/src/instructions/close_trading_credit.rs:54` is the load site). The
  non-delegated branch this entry cleared as correct is therefore the branch that always
  fails on-chain, and the button is offered in exactly that branch
  (`frontend/src/components/trading/session-panel.tsx:312-333`). `S1-X03` owns the
  frontend-side remediation.

**Declared blind spots.** `frontend/node_modules/` does not exist at the freeze SHA, so
`npx tsc --noEmit`, `npm run lint` and any runtime behaviour of `marked`,
`@phantom/react-sdk` or `mermaid` could not be executed; every claim above about those
dependencies is from their published contracts and from this repo's call sites, not from
running them. No signer was held, so no finding rests on a transaction being submitted:
`S9-01`, `S9-06`, `S9-07` and `S9-08` are static-control-flow claims proven by
enumeration of every call site, and `S9-02` rests on a read-only two-layer account read
whose output is pasted above. Two exported hooks are mounted by nothing
(`frontend/src/hooks/use-price-history.ts:23` and
`frontend/src/hooks/use-positions.ts:32`); dead exports are outside the calibration and
are recorded here rather than filed.
